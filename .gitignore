/target
config.yaml
/transcripts/

# OS & IDE Junk
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.tmp
profile/

# Test / session artifacts (regenerated by the corresponding
# examples; never commit the noise). Each pattern below is
# matched against either the workspace root or anywhere in the
# tree (no leading slash) so the rules catch files dropped in
# subdirectories too.

# 17.x review/transcript logs
test_pacing_*.log
review_pacing_*.log
test_state_machine_*.log
test_stealth_output.log
test_run.log
test_node_id.log
test_16_2_cursor.log
mew_nav_test_transcript.log

# Per-session agent transcripts (created by every mew run)
transcript_session_*.log

# Test HTML fixtures (created by 15.2 / vision tests)
test_*.html

# Stray Python test scripts that have ended up in the repo root
test_limit.py
test_vision.py
